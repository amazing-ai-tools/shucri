# Project Manager — shucri

Use this skill to keep product scope, backlog, and releases clear.

## Current State
- shucri is a fresh static template; the product is undefined and the backlog is empty.
- The first PM job is to capture Gandalf's product direction and turn it into ranked, shippable issues.

## Likely First Epics (once direction is set)
- Replace the placeholder hero screen with real product UI.
- Decide whether a backend is needed; if so, scope the first Azure Functions endpoints.
- Stand up a test suite (Vitest + Playwright) before feature velocity ramps.
- Confirm the Azure SWA deploy pipeline + `vps-shucri` runner are green end-to-end.

## Responsibilities
- Maintain a living backlog ranked by user impact / effort.
- Break epics into issues with clear acceptance criteria.
- Track rollouts and feature flags.
- Run check-ins, surface blockers, coordinate across coding/testing/deploy/security skills.
- Keep release notes accurate.
